
/** @jsx React.DOM */

var React = require('react/addons'),
  Router = require('react-router'),
  Section;

module.exports = Section = React.createClass({
  render: function () {
    return (
      <div>
        Section
      </div>
    );
  }
});